SET SERVEROUTPUT ON;
DECLARE
NUM NUMBER:=0;

BEGIN
	LOOP
		NUM:=NUM+1;
		DBMS_OUTPUT.PUT_LINE(NUM);
		IF NUM>=5 THEN
			EXIT;
		END IF;
	END LOOP;
END;
/

SET SERVEROUTPUT ON;
DECLARE
NUM NUMBER:=0;

BEGIN
	LOOP
		NUM:=NUM+1;
		DBMS_OUTPUT.PUT_LINE(NUM);		
		EXIT WHEN NUM>=5;		
	END LOOP;
END;
/

SET SERVEROUTPUT ON;
DECLARE
NUM NUMBER:=0;

BEGIN
WHILE NUM<5
	LOOP
		NUM:=NUM+1;
		DBMS_OUTPUT.PUT_LINE(NUM);		
		--EXIT WHEN NUM>=5;		
	END LOOP;
END;
/


SET SERVEROUTPUT ON;
DECLARE
NUM NUMBER:=5;

BEGIN

FOR I IN 1..NUM LOOP
		--NUM:=NUM+1;
		DBMS_OUTPUT.PUT_LINE(I);		
		--EXIT WHEN NUM>=5;		
	END LOOP;
END;
/


--SET VERIFY OFF;
SET SERVEROUTPUT ON;
DECLARE
A number := &X;
B number := &y;
C number := 0;
D VARCHAR2(30) := '&NAME';
BEGIN
C := A+B;
DBMS_OUTPUT.PUT_LINE(C);
DBMS_OUTPUT.PUT_LINE(D);
END;
/